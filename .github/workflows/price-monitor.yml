name: Acompanhamento pessoal

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "price-monitor/config.json"
      - "price-monitor/src/**"
      - "price-monitor/tests/**"
      - ".github/workflows/price-monitor.yml"
  schedule:
    - cron: "17 * * * *"

concurrency:
  group: personal-monitor
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  scan:
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: price-monitor/requirements.txt

      - name: Dependências
        run: python -m pip install -r price-monitor/requirements.txt

      - name: Testes
        env:
          PYTHONPATH: price-monitor/src
        run: python -m pytest -q price-monitor/tests

      - name: Atualização
        env:
          PYTHONPATH: price-monitor/src
        shell: pwsh
        run: |
          python -m price_monitor.run --config price-monitor/config.json --output docs/data/current.json --history price-monitor/data/history.json
          Copy-Item docs/data/current.json price-monitor/site/data/current.json -Force

      - name: Persistir dados
        shell: pwsh
        run: |
          git config user.name "personal-monitor-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add price-monitor/data/history.json price-monitor/site/data/current.json docs/data/current.json
          git diff --cached --quiet
          if ($LASTEXITCODE -ne 0) {
            git commit -m "data: atualizar acompanhamento pessoal [skip ci]"
            git pull --rebase origin main
            git push origin main
          } else {
            Write-Host "Nenhuma alteração para salvar."
          }

      - name: Resumo
        shell: pwsh
        run: |
          $d = Get-Content docs/data/current.json -Raw | ConvertFrom-Json
          "## Acompanhamento pessoal" >> $env:GITHUB_STEP_SUMMARY
          "- Itens observados: $($d.observed_count)" >> $env:GITHUB_STEP_SUMMARY
          "- Produtos listados: $($d.product_count)" >> $env:GITHUB_STEP_SUMMARY
          "- Itens em vistoria encontrados: $($d.tracked_count)" >> $env:GITHUB_STEP_SUMMARY
          "- Destaques confirmados: $($d.active_count)" >> $env:GITHUB_STEP_SUMMARY
          "- Atualizado: $($d.generated_at)" >> $env:GITHUB_STEP_SUMMARY
